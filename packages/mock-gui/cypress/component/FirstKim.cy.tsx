import { expect } from 'chai';
import MockGui from '../../src/components/kim/MockGui';

describe('MockGui', () => {
  it('renders label correctly', () => {
    // cy.mount(<FirstKim title="확인" />);
    // cy.contains("확인").should("be.visible");
    expect(1).equal(1);
  });
});
